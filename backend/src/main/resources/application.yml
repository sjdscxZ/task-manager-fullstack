server:
  port: 8080
spring:
  application:
    name: task-manager
  datasource:
    url: ${DB_URL:jdbc:h2:mem:tasks;DB_CLOSE_DELAY=-1;MODE=PostgreSQL}
    username: ${DB_USER:sa}
    password: ${DB_PASS:}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
